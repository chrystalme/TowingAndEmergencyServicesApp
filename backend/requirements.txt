fastapi==0.115.0
# The websockets extra is REQUIRED, not optional: bare uvicorn ships no
# WebSocket protocol implementation, logs 'Unsupported upgrade request', and
# answers every handshake with 404 - including a perfectly valid one.
uvicorn[standard]==0.30.6
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.2
pydantic-settings==2.5.2
python-jose==3.3.0
cryptography==42.0.5
python-multipart==0.0.22
fastapi-users==15.0.5
fastapi-users-db-sqlalchemy==7.0.0
argon2-cffi==25.1.0
email-validator==2.3.0
psycopg2-binary==2.9.9
# Pub/sub fan-out across API instances (app/core/broker.py). redis-py
# ships the asyncio client in the same package.
redis==5.2.1
# Server-side FCM. Only used when FIREBASE_CREDENTIALS_JSON is set; the app
# runs without it and simply skips push (see app/services/push.py).
firebase-admin==6.6.0