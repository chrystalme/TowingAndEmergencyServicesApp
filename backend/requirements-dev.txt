# Test-only dependencies.
#
# app/tests/conftest.py imports pytest, pytest_asyncio, httpx and drives an
# in-memory aiosqlite engine — none of which appear in requirements.txt, so the
# declared dependencies alone could never run the test suite.
#
#   pip install -r requirements-dev.txt
#   PYTHONPATH=. pytest -v --tb=short

-r requirements.txt

pytest==8.3.3
# pytest.ini sets asyncio_mode = auto, which requires this plugin.
pytest-asyncio==0.24.0
# conftest.py uses httpx.AsyncClient + ASGITransport to drive the app.
httpx==0.27.2
# Tests run against sqlite+aiosqlite:///:memory: rather than Postgres.
aiosqlite==0.20.0
